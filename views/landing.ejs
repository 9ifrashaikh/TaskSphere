<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TaskSphere</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        header {
            background-color: #333;
            color: #fff;
            padding: 1em 0;
            text-align: center;
        }

        section {
            text-align: center;
            padding: 2em;
        }

        h1 {
            color: #333;
        }

        p {
            color: #555;
        }

        .button-container {
            position: fixed; /* Fixed positioning, relative to the viewport */
            top: 10px; /* Distance from the top */
            right: 40px;
        }

        .signin-button,
        .login-button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #4CAF50;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
            transition: background-color 0.3s;
            margin-right: 10px; /* Adjusted margin between buttons */
        }

        .login-button {
            margin-right: 0; /* No margin for the last button */
        }

        .signin-button:hover,
        .login-button:hover {
            background-color: #45a049;
        }
    </style>
</head>
<body>

<header>
    <h1>TaskSphere</h1>
</header>

<div class="button-container">
    <a href="#" class="signin-button">Sign In</a>
    <a href="#" class="login-button">Log In</a>
</div>

<section>
    <h2>Organize Your Tasks Effectively</h2>
    <p>TaskSphere simplifies task management for individuals and teams. Stay organized and boost productivity.</p>
</section>

</body>
</html>
